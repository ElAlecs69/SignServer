Coloca aqui el certificado TLS real y su clave privada:

- server.crt
- server.key

El certificado debe cubrir el nombre DNS publico usado por los clientes. No
uses certificados autofirmados en produccion. Este directorio esta ignorado
por Git salvo este README.
